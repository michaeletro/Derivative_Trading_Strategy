/**
 * Enumeration constants to define when the auto coloring should be resolved.
 */
export declare enum EAutoColorMode {
    Never = "Never",
    Once = "Once",
    OnAddRemoveSeries = "OnAddRemoveSeries",
    Always = "Always"
}
